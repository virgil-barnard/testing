{
  "name": "webgpu-seg",
  "private": true,
  "type": "module",
  "scripts": {
    "dev":     "vite",
    "build":   "vite build",
    "preview": "vite preview",
    "deploy":  "npm run build && gh-pages -d dist -b gh-pages"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "gh-pages": "^6.0.0"
  }
}
